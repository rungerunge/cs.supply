/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['api.lis-skins.ru'], // Add the domain for skin images
  },
}

module.exports = nextConfig 